name: Regression Tests

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

defaults: 
  run:
    working-directory: build

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: configure
      run: cmake ..
    - name: build
      run: make
    - name: run unit tests
      run: ./tests    

    # simulator runs
    - name: run test-tiny
      run: ./cgraphitti -c ../configfiles/test-tiny.xml
    - name: verify test-tiny
      run: diff ../Output/Results/test-tiny-out.xml ../Testing/RegressionTestComparison/test-tiny-out.xml

    - name: run test-small
      run: ./cgraphitti -c ../configfiles/test-small.xml
    - name: verify test-small
      run: diff ../Output/Results/test-small-out.xml ../Testing/RegressionTestComparison/test-small-out.xml

    - name: run test-small-connected
      run: ./cgraphitti -c ../configfiles/test-small-connected.xml
    - name: verify test-small-connected
      run: diff ../Output/Results/test-small-connected-out.xml ../Testing/RegressionTestComparison/test-small-connected-out.xml

